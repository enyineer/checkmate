{
  "name": "@checkmate/backend",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "generate": "bun --env-file=../../.env run drizzle-kit generate:pg"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.0.0",
    "drizzle-orm": "^0.30.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.0",
    "@types/pg": "^8.11.0",
    "@types/bun": "latest"
  }
}
