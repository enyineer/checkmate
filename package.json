{
  "name": "checkmate-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev"
  },
  "devDependencies": {
    "bun": "^1.0.0"
  }
}
