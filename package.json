{
  "name": "checkmate-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "dev": "docker compose up -d && bun run --filter '@checkmate/backend' --filter '@checkmate/frontend' dev",
    "docker:stop": "docker compose down"
  },
  "devDependencies": {
    "bun": "^1.0.0"
  }
}
