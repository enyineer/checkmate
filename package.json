{
  "name": "checkmate-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "dev": "bun run --filter '@checkmate/backend' --filter '@checkmate/frontend' dev"
  },
  "devDependencies": {
    "bun": "^1.0.0"
  }
}
