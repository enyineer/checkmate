import { definePluginMetadata } from "@checkmate-monitor/common";

/**
 * Plugin metadata for the gRPC Health Check backend.
 */
export const pluginMetadata = definePluginMetadata({
  pluginId: "healthcheck-grpc",
});
